#include "fitness.h"

/* Caso 64 seja um numero muito baixo, o jeito será criar uma struct..
struct bigIndividuo{
    uint64_t parte1;
    uint64_t parte2;
}

bigIndividuo individuoTeste;

//E fazer com que o Fit() faça a concatenação duas vezes..
strcat(comando, AplicarOtim(individuoTeste.parte1, 0));
strcat(comando, AplicarOtim(individuoTeste.parte2, 64));

*/
